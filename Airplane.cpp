//
// Created by c6222 on 2018/3/8.
//

#include "Airplane.h"

Airplane::Airplane(const string &status, const string &number, const string &callsign,
                   const string &model, int passengerCapacity, int passenger, int fuel) :fuel(fuel), number(number), callsign(callsign), model(model), passengerCapacity(passengerCapacity) {
    if (status!="Approaching"||status!="Standing at gate");

}
